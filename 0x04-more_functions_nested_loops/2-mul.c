#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 *@a: The first integer
 *@b: The second integer
 *Return: 1 for digit character or 0 for anything else
 */

int mul(int a, int b)

{
return (a * b);
}
